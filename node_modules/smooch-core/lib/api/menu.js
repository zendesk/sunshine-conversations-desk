'use strict';

exports.__esModule = true;
exports.MenuApi = undefined;

var _base = require('./base');

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

/**
 * @class MenuApi
 * @extends BaseApi
 */

var MenuApi = exports.MenuApi = function (_BaseApi) {
    _inherits(MenuApi, _BaseApi);

    function MenuApi() {
        _classCallCheck(this, MenuApi);

        var _this = _possibleConstructorReturn(this, _BaseApi.apply(this, arguments));

        _this.allowedAuth = ['jwt'];
        return _this;
    }

    /**
     * Fetch an app's menu
     * @return {APIResponse}
     */


    MenuApi.prototype.get = function get() {
        var url = this.getFullURL('menu');
        return this.request('GET', url);
    };

    /**
     * Update an app's menu
     * @param  {object} menuData
     * @return {APIResponse}
     */


    MenuApi.prototype.configure = function configure(menuData) {
        if (!menuData) {
            return Promise.reject(new Error('Must provide props.'));
        }

        if (!menuData.items) {
            return Promise.reject(new Error('Must provide an array of items.'));
        }

        var url = this.getFullURL('menu');

        return this.request('PUT', url, menuData);
    };

    /**
     * Delete an app's menu
     * @return {APIResponse}
     */


    MenuApi.prototype.remove = function remove() {
        var url = this.getFullURL('menu');
        return this.request('DELETE', url);
    };

    return MenuApi;
}(_base.BaseApi);